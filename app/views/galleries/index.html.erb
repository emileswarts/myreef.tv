<%= provide(:title, "Test") %>

<h1>Gallery</h1>

<%= will_paginate %>
<ul class='gallery'>
	<% @galleries.each do |gallery| %>
		<%= image_tag gallery.photo.url(:thumb) %>
	<% end %>
</ul>

<%= will_paginate %>
