<%= provide(:title, "Gallery") %>

<h1>Gallery</h1>

<%= will_paginate %>
<ul class='gallery'>
	<% @galleries.each do |gallery| %>
		<%= link_to (image_tag gallery.photo.url(:thumb), :alt => gallery.title, :class => "list_image"), galleries_path(gallery) %>
	<% end %>
</ul>

<%= will_paginate %>
