
<%= form_for @fishtank, :url => fishtanks_path, :html => { :multipart => true } do |form| %>
	<%= form.text_field :title %><br />
	<%= form.file_field :photo %><br />
	<%= form.submit :submit %>
<% end %>
