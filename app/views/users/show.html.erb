<% provide(:title, @user.name) %>

<div class="row">
	<aside class="span4">
		<section>
		<h1>
			<%= gravatar_for @user, 100 %>
			<%= @user.name %>
		</h1>
		<%= @user.email %>
		</section>
	</aside>
	<h2>1. Add a fish tank</h2>
	<h2><%= link_to "Add a gallery to your tank",  :controller => :galleries, :action => :new  %></h2>
</div>
